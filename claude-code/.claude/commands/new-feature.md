---
description: Scaffold a complete new feature end-to-end (backend + frontend)
allowed-tools: Read, Write, Edit, Bash, Glob
---

Scaffold a complete new feature: $ARGUMENTS

<!--
  CUSTOMIZE THIS FILE:
  Replace the phases below with your actual project structure.
  Delete phases that don't apply (e.g. remove "Database Migration" if you don't use one).
  Add phases specific to your stack (e.g. "Add API route", "Write tests", etc.)
-->

## Phase 1 — Database / Data Layer

1. Create the migration or schema change for this feature
2. Include all necessary tables, indexes, and constraints
3. Show the migration for review before proceeding

## Phase 2 — Backend / API Layer

4. Create the service/controller/handler for this feature
5. Follow the project's layered architecture (see skills/architecture)
6. Add input validation and error handling

## Phase 3 — Frontend / UI Layer

7. Create the UI components for this feature
8. Wire up data fetching using the project's standard pattern
9. Add loading and error states

## Phase 4 — Navigation / Routing

10. Register the new route or page in the app's navigation

Pause between phases for review and approval.
